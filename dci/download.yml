---
- name: check Atomic Host file exists
  stat:
    path: '{{ atomic_image_path }}'
  register: atomic_host_file

- name: download component if not exists
  dci_component:
    dest: '{{ atomic_image_path }}'
    id: '{{ component["id"] }}'
  when: atomic_host_file.stat.exists == False
